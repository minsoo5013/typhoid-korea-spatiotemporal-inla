#!/usr/bin/env Rscript

# EXPLORATORY ONLY -- used during development to inspect candidate predictors.
# NOT used for the final analysis. Do NOT source. The final model uses a fixed,
# pre-specified set of six covariates (see R/03_model_fit.R).

stop(
  "Exploratory scripts are not part of the public final-model pipeline. ",
  "Do not source this directory when reproducing the manuscript model."
)
