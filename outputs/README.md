# Outputs

- `reference/`: locked manuscript reference tables copied from the final contiguity M6 analysis.
- `generated/`: local rerun outputs. This directory is ignored by git by default.

The reference values are provided for validation and audit. Refit outputs should not replace manuscript values without documented review and approval.
